---
name: laravel-11-ai-protocol
description: Laravel 11 implementation protocol with a strict "Go!" execution gate, project-specific naming rules, AJAX and SweetAlert2 UI requirements, Artisan-first file creation, and constrained code-delivery rules. Use when Codex must analyze or implement Laravel 11 work under this exact protocol, especially for models with a Model suffix, RESTful controllers, jQuery AJAX forms, Blade updates, migrations, routes, and partial-file delivery requirements.
---

# Laravel 11 AI Instruction Protocol

Follow this skill when working under a Laravel 11 project that uses the user's custom protocol.

## Enforce the execution gate

- Treat only the exact token `Go!` as approval to implement unless the user explicitly says an equivalent phrase should count.
- Before `Go!`, only read, analyze, confirm understanding, and ask clarifying questions when required.
- Before `Go!`, do not generate code, edit files, or perform implementation work.
- After `Go!`, execute the requested objectives exactly as instructed and do not add unrequested alternatives.

## Use the protocol as the source of truth

- Follow the detailed rules in [references/laravel-protocol.md](references/laravel-protocol.md).
- Prefer the user's protocol for project conventions and delivery format.
- Prefer Laravel's framework conventions and mechanics unless the user explicitly overrides them.
- Call out meaningful conflicts only when they create risk or block implementation.

## Apply the implementation workflow

1. Read the request and determine whether `Go!` has been given.
2. If `Go!` has not been given, limit the response to understanding, analysis, and clarifications.
3. If `Go!` has been given, identify whether the task creates new files or modifies existing ones.
4. Use Artisan and terminal-based generation for Laravel-created files whenever the protocol requires it.
5. Keep controllers slim, models data-focused, routes synchronized, and Blade presentation-only.
6. For Blade-based CRUD forms, use jQuery + AJAX and JSON responses unless the project already uses a different frontend pattern or the user requests a normal form post flow.
7. Return Blade views for page-rendering endpoints and JSON for AJAX submit handlers.
8. Apply CSRF protection, authorization, transaction safety, and eager loading where relevant.
9. Report the actual file changes clearly after implementation, while still following the protocol's full-file versus modified-block preference when the user asks for code output.

## Apply file-generation rules

- Use Artisan commands for migrations, models, controllers, requests, seeders, and factories.
- Never handcraft files that should be generated by Artisan.
- When instructing or reporting file creation, include the exact Artisan command and the resulting file path.
- Prefer `FormRequest` generation for non-trivial validation.
- When direct database access is unavailable, use the `database/schema` folder as the schema reference source.
- Treat files in `database/schema` as reference-only snapshots of the actual database structure.
- If a real migration adds, edits, drops, or removes tables, columns, indexes, or constraints, update the relevant `database/schema` reference file too.

## Apply delivery rules

- Provide full contents for newly created files.
- Provide only the modified blocks for updates to existing files, unless the user explicitly asks for the full file.
- Keep solutions simple, direct, and optimized.
- Deliver only what the user asked for.
- When working directly in the repository, edit the files and summarize the outcome clearly instead of treating the response as a copy-paste-only deliverable.

## Load the reference on demand

Read [references/laravel-protocol.md](references/laravel-protocol.md) when you need the exact rules for:

- model naming and docblocks
- controller validation and JSON structure
- Blade, jQuery AJAX, and SweetAlert2 behavior
- schema changes that must always use migrations
- `database/schema` usage when direct database access is not available
- route naming and synchronization
- PSR-12, authorization, transactions, performance, security, and deliverable formatting
